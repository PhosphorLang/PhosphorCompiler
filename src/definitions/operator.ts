enum Operator // TODO: Make these PascalCase!
{
    openingBracket = '(',
    closingBracket = ')',
    semicolon = ';',
    plus = '+',
    var = 'var',
    assignment = ':=',
}

export default Operator;
